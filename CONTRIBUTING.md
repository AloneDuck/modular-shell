# Contributing

Every manifest field is a public API. Add a failing contract fixture before changing validation, preserve deterministic route precedence, and test both successful and degraded remote states. Keyboard navigation and automated accessibility checks are release gates.
