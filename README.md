# Modular Shell

A contract-driven microfrontend host. It validates a signed runtime manifest, isolates route ownership, checks host compatibility, and keeps one failed remote from taking down global navigation.

Run `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run test:e2e` before changing the public manifest contract.
